{
  "linkedin": "A kind 3 follow list is a full snapshot, not an add operation.\n\nNIP-02 says every new list overwrites the past ones. NIP-01 makes kind 3 replaceable by pubkey and kind, with created_at selecting the latest event and the lower event ID breaking a timestamp tie.\n\nThat makes a stale device capable of erasing newer follows.\n\nWe mapped the safe sequence: fetch the current winner, preserve every p tag, apply the change, publish one complete event, then read it back from more than one relay. Which event ID did your client edit from?\n\nRelays can retain older copies, and a larger list is not automatically the current one.\n\nThe complete-snapshot model:\n{url}\n\n#Nostr #NIP #WebOfTrust #Protocol",
  "nostr": "A kind 3 follow list is a full snapshot, not an add operation.\n\nNIP-02 says each new list overwrites the old one. A stale device can therefore erase newer follows when it signs from an outdated snapshot.\n\nFetch the current winner, preserve every p tag, apply the change, publish the complete event, then read it back. Older copies are not a safe merge source.\n\n{url}"
}
